# Used automatically by `npm run build` (Vite's default mode is "production").
# Empty on purpose: in production the frontend calls the API via relative paths
# (e.g. fetch("/api/generate")), same-origin, proxied by Firebase Hosting rewrites
# to the Cloud Run backend (see firebase.json + docs/DEPLOY.md). This is NOT a secret.
VITE_API_BASE=
